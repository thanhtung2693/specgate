# doc-registry environment — copy to doc-registry.env and fill in values.
#
# REQUIRED — generate with: openssl rand -hex 32
SETTINGS_ENCRYPTION_KEY=

# OPTIONAL — Sentry error monitoring
# SENTRY_DSN=

# OPTIONAL — MCP API key. If unset, doc-registry mints a random key at startup.
# MCP_API_KEY=
